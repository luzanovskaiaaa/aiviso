# Корневой .env — используется docker-compose.yml для подстановки ${POSTGRES_PASSWORD}.
# Скопируй в .env (в gitignore) и проставь свои значения.
POSTGRES_PASSWORD=replace-with-strong-random-password
